# 🏎️ Дорожный прорыв (kart-racer)

Аркадные 3D-гонки на [Three.js](https://threejs.org/): пост-апокалиптичные трассы Приморья, советские тачки, звери на дороге и мини-боссы в кампании.

**Играть онлайн:** [antonlabukhin-hue.github.io/kart-racer](https://antonlabukhin-hue.github.io/kart-racer/)

---

## Скриншоты

| Меню | Гонка | Кампания |
|:----:|:-----:|:--------:|
| `images/menu.jpg` | `images/map_arsenev.jpg` | `images/menu_campaign.jpg` |

*(открой игру локально или по ссылке выше — в README картинки подтянутся с GitHub Pages после деплоя)*

---

## Об игре

- **Кампания** — цепочка трасс до исследовательского центра, свой мини-босс на главу  
- **Одиночный заезд** — выбор карты, погоды (день / ночь / дождь) и сложности  
- **Гараж и магазин** — Чебурашка, Нива, Волга; цвет и тюнинг  
- **Профили** — прогресс, фишки, ачивки и трофеи в `localStorage`  
- **Мобайл** — сенсорное управление, адаптивный HUD  

Сеттинг: лихие 90-е / постапок, звери перебегают дорогу, на трассе — нитро, жвачка «Турбо», масло и кочки.

---

## Управление

| Действие | Клавиатура | Мобильный |
|----------|------------|-----------|
| Газ | `W` / `↑` | Кнопка вперёд |
| Назад / тормоз | `S` / `↓` | Назад |
| Полоса влево / вправо | `A` `D` / `←` `→` | Стрелки |
| Камера | `C` | Кнопка камеры |
| Пауза | `P` / `Esc` | Пауза |

---

## Быстрый старт

```bash
git clone https://github.com/antonlabukhin-hue/kart-racer.git
cd kart-racer
npm install
npm run dev
```

Сайт собирается сам: на каждый пуш в `main` GitHub Actions прогоняет тесты, запускает `npm run build` и публикует папку `dist/` на GitHub Pages (`.github/workflows/deploy.yml`). Если тесты или сборка упали, сайт не обновится, а у коммита в GitHub будет красный крестик.

Версии библиотек (three, vite) записаны только в `package.json`.

**Требования:** Node.js 20.19+ или 22.12+, современный браузер с WebGL.

---

## Модули (фаза 1 разбиения)

Исходники постепенно выносятся из монолита `index.html` в `src/`:

- `audio.js` — SoundEngine  
- `animals.js` — AnimalSpawner  
- `particles.js` — ParticleSystem  
- `data.js` — кампания, машины, животные (пока используется только в `animals.js`)  
- `boss.js` — заглушка (меши ещё в initGame)  

Подробный план: [`src/ARCHITECTURE.md`](src/ARCHITECTURE.md).  
Модули подключены из `index.html` как ES modules.

## Структура репозитория

```text
kart-racer/
├── index.html          # основной билд игры (монолит + Three.js)
├── src/                # модули (audio, animals, particles, data)
├── tests/unit/         # быстрые тесты модулей из src/ (Vitest)
├── tests/e2e/          # тесты игры в браузере: вход, заезд, кампания, пауза (Playwright)
├── public/images/      # меню, карты, трофеи, спрайты машин
├── public/music/       # menu-music.mp3, race-music.mp3
├── package.json        # версии three и vite
├── vite.config.js      # настройки сборки
├── .github/workflows/  # сборка и публикация на GitHub Pages
└── README.md
```

Картинки и музыка лежат в `public/`: Vite копирует их в сборку как есть. В коде адреса без `public/`: `images/menu.jpg`, `music/race-music.mp3`.

---

## Стек

| | |
|--|--|
| 3D | [Three.js](https://threejs.org/) `^0.185` |
| Сборка | [Vite](https://vitejs.dev/) `^8.2` |
| Аудио | Web Audio API (двигатель, SFX) + HTML5 Audio (музыка) |
| Сохранения | `localStorage` (профили, кампания, гараж) |

Внешних серверов и аккаунтов нет — всё работает офлайн после загрузки.

---

## Режимы и контент

- **Карты:** Арсеньев, Промзона, Свалка и др.  
- **Машины:** Чебурашка (старт), Нива, Волга  
- **Сложность:** лёгкая / средняя / сложная  
- **Качество графики:** low / medium / high (тени, плотность декора)  

Прогресс кампании и гаража привязан к **имени профиля** на устройстве.

---

## Известные ограничения

- В sandbox / `about:srcdoc` музыка и картинки с чужого origin могут блокироваться CORS — открывай через GitHub Pages или `npm run dev`.  
- Процедурная музыка включается, если MP3 недоступен.  
- Мультиплеер в меню — заглушка («в разработке»).

---

## Разработка

Имеет смысл править один «источник правды» (корневой `index.html` *или* модули под Vite) и не плодить расхождения. Перед пушем:

```bash
npm run dev
# пройти заезд и проверить консоль браузера на ошибки
npm test
# тесты: сами соберут игру, откроют её в браузере и пройдут вход, заезд, кампанию и паузу
```

Перед первым `npm test` один раз скачать браузер для тестов: `npx playwright install chromium`. Если тест упал, в `test-results/` лежит скриншот момента падения. Тесты же запускаются в GitHub на каждый PR.

Коммиты лучше с коротким смыслом: `fix: traffic maxCars in campaign`, `perf: compress menu JPEGs`.

---

## Лицензия

ISC (см. `package.json`). Ассеты в `public/images/` и `public/music/` — для этого проекта; при переиспользовании уточняй происхождение файлов.

---

## Ссылки

- **Репозиторий:** [github.com/antonlabukhin-hue/kart-racer](https://github.com/antonlabukhin-hue/kart-racer)  
- **Демо:** [antonlabukhin-hue.github.io/kart-racer](https://antonlabukhin-hue.github.io/kart-racer/)  

Идеи, баги и PR приветствуются.
